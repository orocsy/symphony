name: ownership-boundary
description: "Tenant/workspace ownership helpers and invariant prompts extracted from multi-owner data safety rules."
source: reusable patterns
provides:
  - tenant-scoped type helper
  - cross-tenant guard helper
  - boundary documentation seed
default_decision: "Use only when the project has tenants, workspaces, teams, or account-owned data."
reject_when:
  - project has no multi-owner data boundary
