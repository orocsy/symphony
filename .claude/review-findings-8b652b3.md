# Review findings — integration branch 8b652b3 (OXE-0.1 implementation)

Diff: 8883315..8b652b3 in worktree /private/tmp/orocsy-symphony-openai-extension-integration
Gate: BLOCK — 3 × P1

| # | Severity | File:Line | Reviewer | Issue | Proposed Fix |
|---|----------|-----------|----------|-------|--------------|
| 1 | P1 | elixir/lib/symphony_elixir/extensions_audit.ex:58,321 | deep (verified live) | Inherited GIT_DIR/GIT_WORK_TREE/GIT_ALTERNATE_OBJECT_DIRECTORIES redirect every git call to another repository; worktree guard still passes (—show-toplevel follows -C) → audit reports clean PASS against the wrong repo. Reproduced twice (module-level and shell-level). Git hooks set GIT_DIR, and README pitches the task as a migration guard. | Extend @git_env with nil entries (System.cmd unsets on nil) for GIT_DIR, GIT_WORK_TREE, GIT_COMMON_DIR, GIT_OBJECT_DIRECTORY, GIT_ALTERNATE_OBJECT_DIRECTORIES, GIT_INDEX_FILE, GIT_NAMESPACE, GIT_CEILING_DIRECTORIES, GIT_DISCOVERY_ACROSS_FILESYSTEM; add regression test setting GIT_DIR |
| 2 | P1 | extensions_audit_test.exs:119,140; extensions_audit_task_test.exs:13,23,38; .github/workflows/make-all.yml:17 | test + spec (verified: workflow has no fetch-depth override) | 5 tests run against the ambient repo and assert the pinned baseline resolves; CI checkout@v4 defaults to depth-1 shallow clone → cat-file fails → 5 red tests on the first PR of this branch. Also collides with README's own statement that old-lineage branches legitimately fail the audit. | Convert to hermetic fixtures (machinery exists at extensions_audit_test.exs:274-300); keep ≤1 opt-in smoke test; or set fetch-depth: 0 for this branch's CI |
| 3 | P1 | extensions_audit_task_test.exs:66-68 | test (verified: ANSI wraps stderr when TTY) | Exact-equality assertion on stderr that Mix.Shell.IO.error ANSI-colorizes in a terminal → suite red for any developer running mix test locally; passes only non-TTY. strip_ansi/1 exists in test/support/snapshot_support.exs:51, unused here. | Strip ANSI (existing helper) or disable ansi_enabled in test_helper before asserting |
| 4 | P2 | extensions_audit.ex:234,285,312 | deep | Non-zero git exit conflated with semantic mismatch codes; raw `fatal:` stdout+stderr spliced into findings (actual:/detail:). Partial clone (--filter=tree:0) → reported as :baseline_tree_mismatch ("tampering") instead of missing-objects guidance; leaks paths/URLs, breaking the trace's no-config-leak promise. | Branch on exit status: 128/unexpected → :git_unavailable or :baseline_object_missing; mismatch codes only for status 0 (merge-base: status 1 = not-ancestor) |
| 5 | P2 | extensions_audit.ex:324-326 | deep (verified: CaseClauseError caught) | `rescue in ErlangError` catches ALL Erlang runtime errors incl. CaseClauseError from the single-clause case → wrong-shaped injected git or internal bug becomes a normal-looking :git_unavailable finding; fixture mistakes make tests pass while covering nothing. | Rescue only around git.(...); match %ErlangError{original: :enoent}; reraise everything else with __STACKTRACE__ |
| 6 | P2 | extensions_audit.ex:68 | deep (probed yaml_elixir) | Duplicate top-level keys (first wins) and multi-document YAML (last doc wins) silently accepted — a manifest whose visible bottom entry differs from what the audit verifies defeats the "machine authority" purpose of the strict schema. | YamlElixir.read_all_from_string/1 + require exactly one document; scan source for duplicate top-level keys or document the limitation |
| 7 | P2 | elixir/lib/mix/tasks/extensions.audit.ex:2 | deep (verified mix help) | @moduledoc false removes the task from `mix help` (shortdoc alone insufficient) — sole deviation from all three sibling tasks; a README-advertised command is undiscoverable. | Real @moduledoc heredoc with Usage block, @shortdoc ordering per siblings |
| 8 | P2 | extensions_audit_test.exs:151-153 | test | "never invokes fetch/checkout/…" is a blacklist; new mutating subcommands (gc, worktree, update-ref, remote) pass silently. | Assert allowlist: subcommand ∈ rev-parse/cat-file/merge-base/rev-list |
| 9 | P2 | extensions_audit_test.exs:264-272 | test | Fixture repos inherit global git config — this machine's core.hooksPath runs real pre/post-commit hooks inside fixtures; gpgsign/template would break them. | Set GIT_CONFIG_GLOBAL=/dev/null, GIT_CONFIG_SYSTEM=/dev/null, -c commit.gpgsign=false, -c core.hooksPath= in fixture helper |
| 10 | P2 | extensions_audit_test.exs:119-131 | test + spec | "accepts a linked git worktree" never constructs a linked worktree — passes vacuously in a normal clone; the trace names the pointer-file case load-bearing. | Fixture + `git worktree add`, assert File.regular?(.git) before verify |
| 11 | P2 | extensions_audit.ex:91-95 | deep + test | File.read :eacces/:eisdir/:enotdir mislabeled :manifest_invalid_yaml ("could not be decoded") — permissions problem reported as YAML syntax; untested. | Separate :manifest_unreadable finding with :file.format_error/1 detail; keep YAML arm for ParsingError (surface line/column) |
| 12 | P3 | extensions_audit.ex:325 | deep | :enoent detail renders "Erlang error: :enoent" — meaningless to operators | Map to "git executable not found on PATH" |
| 13 | P3 | extensions.audit.ex:9 | deep | @shortdoc promises "and extension patch budget" — deferred to OXE-0.2; --only budget raises | Drop clause until it exists |
| 14 | P3 | extensions_audit.ex:294 | deep | rev-list --first-parent HEAD materializes full chain each run | Bound: rev-list --first-parent HEAD ^<commit>~1 |
| 15 | P3 | extensions_audit.ex:47,88,150-157 | deep | Report.findings dead (always []); head never surfaced by task; repository/version/spec_status validated but never used in any check | Drop dead fields or use them; consider version cross-check |
| 16 | P3 | extensions_audit.ex:197-204 | deep | canonicalize failure indistinguishable from wrong root in :not_a_git_worktree | Propagate reason into detail |
| 17 | P3 | extensions_audit.ex:146 | test | Only first invalid manifest field reported — behavior unpinned by tests | Pin with multi-invalid-field test or report all |
| 18 | P3 | extensions_audit_test.exs | test | Untested edges: annotated-tag object (code verified correct, test absent), real-subdir repo_root, unborn-HEAD non-SHA path, task --repo-root nonexistent/relative | Add edge tests |
| 19 | P3 | elixir/README.md | deep | GIT_NO_LAZY_FETCH ignored by git < 2.36 — offline guarantee silently degrades | Document minimum git version |
| 20 | P3 | openai_extension_miu_technical_design.md:437-439 | spec | "The implementation also updates the parent architecture…" describes edits that pre-existed the diff | Reword to past/pre-existing |

Owner decisions (not code defects):
- A. Branch-model deviation: integration branch is linear from baseline + cherry-picked docs; the Orocsy-main merge required by the MIU precondition has NOT happened (Codex surfaced this as an Open row). Decide: bless deferred-merge sequencing (update branch model + precondition wording) or perform the merge before further MIUs.
- B. Trace corpus remains single-host (checksummed in-place receipt only; off-host quarantine needs owner approval per receipt).

Validation evidence: 21/21 new tests pass (non-TTY); audit emits byte-exact promised output on integration branch; negative control exits 1 with typed finding; format/credo --strict/specs.check pass; 100% line coverage on both new modules; full-suite failures (2-4, run-varying) are only in untouched upstream ssh/core timing tests, pre-documented by Codex as pre-existing.
